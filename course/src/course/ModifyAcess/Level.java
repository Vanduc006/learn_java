package course.ModifyAcess;

public class Level {
    public int a;
    int b; // don't use default
    protected int c;
    private int d;
}

// public -> same class; package; subclass, universe
// default -> same class; package
// private -> same class
// protected -> same class, package, subclass